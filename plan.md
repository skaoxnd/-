# 🗺️ 구현 계획서 (Implementation Plan)

본 계획서는 `cloud.md`를 기반으로 2일 내에 프로젝트를 완성하기 위한 구체적인 작업 체크리스트입니다.

## Phase 1: 개발 환경 및 뼈대 구축 (Day 1 - 오전)
- [ ] **프로젝트 초기화**: `npx create-next-app@latest .` 명령어를 통해 Next.js App Router 프로젝트 생성.
- [ ] **라이브러리 설치**: `recharts`, `framer-motion`, `lucide-react` 설치.
- [ ] **글로벌 설정**: `tailwind.config.ts` 및 `globals.css`에 다크 테마 컬러 팔레트와 커스텀 폰트(Pretendard, Mono) 설정.
- [ ] **폴더 구조 생성**: `/components`, `/data`, `/app/stage1~3` 등 필요 디렉토리 생성.

## Phase 2: 데이터 셋업 및 공통 UI 컴포넌트 개발 (Day 1 - 오후)
- [ ] **가짜 데이터 작성 (`data/mockData.ts`)**: 심슨의 역설, 지리적 프로파일링, 벤포드의 법칙에 필요한 로우 데이터 및 통계 유틸리티 작성.
- [ ] **레이아웃 템플릿 구현 (`app/layout.tsx`)**: 좌측(스토리 브리핑), 우측 상단(데이터 시각화), 우측 하단(조작 패널)으로 나뉘는 3단 반응형 레이아웃 구축.
- [ ] **공통 컴포넌트 개발**:
  - `NarrativeBox.tsx`: Framer Motion을 활용한 텍스트 타이핑 애니메이션 패널.
  - `ControlPanel.tsx`: 각종 스위치, 슬라이더, 입력창이 포함된 수사 콘솔 패널.

## Phase 3: 스테이지별 핵심 로직 연동 (Day 2 - 오전)
- [ ] **인트로 (`app/page.tsx`)**: 타자기 애니메이션 브리핑 및 `[시스템 접속]` 버튼을 통한 Stage 1 이동 로직.
- [ ] **Stage 1 (`app/stage1/page.tsx`) - 공포 본능**:
  - `CustomBarChart.tsx` 구현 (Stacked Bar 차트).
  - 콘솔의 토글 스위치 조작 시 범죄 유형별로 데이터가 분할되는 애니메이션 및 상태 변경 로직.
- [ ] **Stage 2 (`app/stage2/page.tsx`) - 직선 본능**:
  - `MapScatter.tsx` 구현 (2D 산점도 좌표 차트).
  - 슬라이더 드래그 시 버퍼존 반경(`bufferRadius`)이 넓어지고, 외곽 좌표에 붉은색 타겟이 렌더링되는 로직.
- [ ] **Stage 3 (`app/stage3/page.tsx`) - 단일 관점 본능 & 클라이맥스**:
  - 막대그래프 위에 벤포드 자연 확률 곡선(Line Chart)을 오버레이로 렌더링.
  - **키보드 입력 기능**: 용의자 결백을 논파한 후, 최종 범인의 코드를 키보드로 입력하고 검증하는 폼 구현. 정답 시 엔딩 크레딧 전환.

## Phase 4: 폴리싱, 테스트 및 배포 (Day 2 - 오후)
- [ ] **디테일 폴리싱**: 차트 렌더링 애니메이션 및 트랜지션 부드럽게 최적화.
- [ ] **반응형 테스트**: PC 뷰뿐만 아니라 모바일/태블릿 해상도에서도 3단 레이아웃이 유연하게 렌더링되는지 확인.
- [ ] **배포**: Vercel에 GitHub 레포지토리 연동 및 프로덕션 빌드/배포.
- [ ] **라이브 테스트**: 배포된 링크로 접속하여 스테이지 1~3 흐름 및 키보드 입력 정답 처리가 잘 되는지 최종 점검.
